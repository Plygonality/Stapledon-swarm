# Schema

Enforceable contracts. CI validates every `constraints/*.json` file against [`constraints.schema.json`](constraints.schema.json).

| File | Job |
| --- | --- |
| [`constraints.schema.json`](constraints.schema.json) | JSON Schema for constraint files. |
| [`status-tags.md`](status-tags.md) | CANON / INFERENCE / OPEN. Human vocabulary the schema enum matches. |

`bible/` is context. It is not this folder. A wiki sentence is not a substitute for a constraint file.
